# Server Configuration
PORT=5000

# MongoDB Database connection URI
# Replace with your local MongoDB URI or MongoDB Atlas connection string
MONGO_URI=mongodb://localhost:27017/freelance-invoice-tracker

# JWT Authentication secret keys
# Generate a secure string (e.g. using openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_key_here
JWT_LIFETIME=30d
